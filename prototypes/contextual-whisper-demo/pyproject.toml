[project]
name = "symborg-contextual-whisper-demo"
version = "0.0.1"
description = "A minimal heuristic prototype for SYMBORG cognitive packets."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
